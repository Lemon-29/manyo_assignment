<article class="admin_index">
  <h2>🖥Management Screen：List of users🖥</h2>
  <% if notice %>
    <p class="alert alert-info"><%= notice %></p>
  <% end %>

  <p class="user_new"><%= link_to 'Create a new user', new_admin_user_path %></p>

  <ul class="tasklist flex">
    <% @users.each do |user| %>
    <li>
      <p><span class="item_name">ID</span> <%= user.id %></p>
      <p><span class="item_name">Name</span> <%= user.name %></p>
      <p><span class="item_name">Number of tasks</span> <%= user.tasks.count %>件</p>
      <div class="btns">
        <div class="innerblock">
          <%= link_to 'Check the details', admin_user_path(user.id), class: "show_user" %>
          <%= link_to "Edit user information", edit_admin_user_path(user.id), data: { confirm: 'You wanna edit？' }, class: "edit_user" %>
          <%= link_to 'Delate user_s information', admin_user_path(user.id), method: :delete ,data: { confirm: 'Are you sure you wanna delete it?' }, class: "delete_user" %>
        </div>
      </div>
      </li>
    <% end %>
  </ul>
  <div class="pagination_block">
    <%= paginate @users %>
  </div>
</article>
